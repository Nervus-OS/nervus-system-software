org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=768m
org.gradle.parallel=true
kotlin.code.style=official

# 目标 ABI 缺省值。命令行 -Pnervus.abi=... 覆盖它。
# 缺省 arm64 是因为交付目标是真机（RK3588）；本机验证时显式传 linux-x86_64
nervus.abi=linux-arm64
